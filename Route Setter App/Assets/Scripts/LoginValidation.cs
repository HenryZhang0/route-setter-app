using Realms.Sync;
using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;
using UnityEngine.SceneManagement;
using UnityEngine.UI;
using System.Threading.Tasks;

public class LoginValidation : MonoBehaviour
{
    //public TMP_InputField emailInput;
    //public TMP_InputField passwordInput;

    /*
    async public Task<Credentials> Login()
    {
        var email = emailInput.text;
        var password = passwordInput.text;

        var errorText = GameObject.Find("ErrorLabel").GetComponent<TMP_Text>();

        if (email == "" || password == "")
        {
            errorText.text = "Input ALL Text Fields";
            return null;
        }
        else
        {
            errorText.text = "";
            return Credentials.EmailPassword(email, password);
        }
    }
    */
}
